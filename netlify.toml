# Netlify Configuration for Ravin-Assistant

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  base = "."
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
