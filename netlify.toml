# Netlify Configuration for Ravin-Assistant

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  base = "."
  command = "npm run build"
  publish = "dist"
  ignore = "exit 0"

[build.environment]
  NODE_VERSION = "18"
